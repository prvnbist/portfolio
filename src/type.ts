export type Timeline = {
   id: string
   url: string
   title: string
   from: string
   to: string
   location: string
   is_current: boolean
   description: string
}

export type Skill = {
   id: string
   title: string
   category: string
   sub_category: string
}
